# Forge Context Archive

This directory stores local safety backups and deprecated runtime archives.

Files here are ignored by default. Force-add archive content only when your team intentionally wants migration history in git.
