# Forge Context Patches

This directory stores reviewable local context patch proposals.

Files here are ignored by default. Force-add a specific patch only when your team intentionally wants to review or commit it.
